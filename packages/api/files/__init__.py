"""File ingestion + summarization. Accepts uploaded files, extracts
text where the format is supported, and returns an LLM-grounded
summary. Supersedes the frontend's `smartMockSummary` canned text."""
